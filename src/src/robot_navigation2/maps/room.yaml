# 地图配置
image: room.pgm  # 地图图像文件名，使用 PGM 格式
mode: trinary  # 地图模式，表示这是一个三元地图
resolution: 0.05  # 地图分辨率，单位为米/像素
origin: [-7.68, -7.28, 0]  # 地图的原点坐标，格式为 [x, y, theta]，这里 theta 表示旋转角度
negate: 0  # 反转图像的占用情况，0 表示不反转
occupied_thresh: 0.65  # 占用阈值，像素值高于该值被认为是占用的
free_thresh: 0.25  # 空闲阈值，像素值低于该值被认为是空闲的
